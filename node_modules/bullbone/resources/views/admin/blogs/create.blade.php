@extends('layout.app')

@section('content')
<style>
    .blog-create-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .page-header {
        margin-bottom: 35px;
        padding-bottom: 20px;
        border-bottom: 3px solid #1a1a1a;
    }

    .page-title {
        font-size: 32px;
        font-weight: 700;
        color: #1a1a1a;
        margin: 0;
        letter-spacing: -0.5px;
    }

    .alert-danger {
        background: #1a1a1a;
        color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 25px;
        border-left: 4px solid #666;
        animation: slideIn 0.3s ease;
    }

    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .alert-danger strong {
        display: block;
        margin-bottom: 10px;
        font-size: 16px;
    }

    .alert-danger ul {
        margin: 10px 0 0 0;
        padding-left: 20px;
    }

    .alert-danger li {
        margin: 5px 0;
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 400px;
        gap: 30px;
    }

    .form-group {
        margin-bottom: 25px;
    }

    .form-label {
        display: block;
        color: #1a1a1a;
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 8px;
        letter-spacing: 0.3px;
    }

    .form-control,
    .form-select {
        width: 100%;
        padding: 12px 16px;
        border: 2px solid #e0e0e0;
        border-radius: 8px;
        font-size: 15px;
        transition: all 0.3s ease;
        background: #fafafa;
        color: #1a1a1a;
        font-family: inherit;
    }

    .form-control:focus,
    .form-select:focus {
        outline: none;
        border-color: #1a1a1a;
        background: #ffffff;
        box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.05);
    }

    textarea.form-control {
        resize: vertical;
        font-family: inherit;
    }

    .form-text {
        display: block;
        margin-top: 6px;
        font-size: 13px;
        color: #666;
    }

    .section-title {
        font-size: 20px;
        font-weight: 700;
        color: #1a1a1a;
        margin: 35px 0 20px 0;
        padding-bottom: 10px;
        border-bottom: 2px solid #e0e0e0;
    }

    .sidebar-card {
        background: #ffffff;
        border: 2px solid #e0e0e0;
        border-radius: 8px;
        margin-bottom: 20px;
        overflow: hidden;
    }

    .card-header {
        background: #1a1a1a;
        color: #ffffff;
        padding: 14px 18px;
        font-weight: 600;
        font-size: 15px;
        letter-spacing: 0.3px;
    }

    .card-body {
        padding: 20px;
    }

    .form-check {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .form-check-input {
        width: 20px;
        height: 20px;
        border: 2px solid #e0e0e0;
        border-radius: 4px;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .form-check-input:checked {
        background-color: #1a1a1a;
        border-color: #1a1a1a;
    }

    .form-check-input:focus {
        outline: none;
        box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.05);
    }

    .form-check-label {
        cursor: pointer;
        font-size: 14px;
        font-weight: 500;
        color: #333;
    }

    .file-input-wrapper {
        position: relative;
    }

    input[type="file"] {
        padding: 10px;
        font-size: 14px;
    }

    .form-actions {
        display: flex;
        gap: 12px;
        margin-top: 35px;
        padding-top: 25px;
        border-top: 2px solid #e0e0e0;
    }

    .btn-primary {
        background: #1a1a1a;
        color: #ffffff;
        border: 2px solid #1a1a1a;
        padding: 14px 32px;
        border-radius: 8px;
        font-size: 15px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn-primary:hover {
        background: #000000;
        border-color: #000000;
        color: #ffffff;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
    }

    .btn-secondary {
        background: #ffffff;
        color: #1a1a1a;
        border: 2px solid #1a1a1a;
        padding: 14px 32px;
        border-radius: 8px;
        font-size: 15px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn-secondary:hover {
        background: #1a1a1a;
        color: #ffffff;
        transform: translateY(-2px);
    }

    @media (max-width: 992px) {
        .form-row {
            grid-template-columns: 1fr;
        }
    }

    @media (max-width: 768px) {
        .blog-create-container {
            padding: 20px 15px;
        }

        .page-title {
            font-size: 26px;
        }

        .form-actions {
            flex-direction: column;
        }

        .btn-primary,
        .btn-secondary {
            width: 100%;
            text-align: center;
        }
    }
</style>

<div class="blog-create-container">
    <div class="page-header">
        <h1 class="page-title">Add New Blog</h1>
    </div>

    @if ($errors->any())
        <div class="alert-danger">
            <strong>Error!</strong> Please fix the issues below.
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif

    <form action="{{ route('admin.blogs.store') }}" method="POST" enctype="multipart/form-data">
        @csrf
        
        <div class="form-row">
            <div>
                <div class="form-group">
                    <label for="title" class="form-label">Blog Title *</label>
                    <input type="text" name="title" id="title" class="form-control" placeholder="Enter blog title" value="{{ old('title') }}" required>
                </div>

                <div class="form-group">
                    <label for="excerpt" class="form-label">Excerpt (Short Summary) *</label>
                    <textarea name="excerpt" id="excerpt" class="form-control" rows="3" placeholder="Brief summary of the blog..." required>{{ old('excerpt') }}</textarea>
                </div>

                <div class="form-group">
                    <label for="content" class="form-label">Content *</label>
                    <textarea name="content" id="content" class="form-control" rows="15" placeholder="Write your blog content here..." required>{{ old('content') }}</textarea>
                </div>

                <h5 class="section-title">SEO Settings</h5>
                
                <div class="form-group">
                    <label for="seo_title" class="form-label">SEO Title</label>
                    <input type="text" name="seo_title" id="seo_title" class="form-control" placeholder="SEO optimized title" value="{{ old('seo_title') }}">
                    <small class="form-text">Leave empty to use blog title</small>
                </div>

                <div class="form-group">
                    <label for="seo_description" class="form-label">SEO Description</label>
                    <textarea name="seo_description" id="seo_description" class="form-control" rows="3" placeholder="SEO meta description">{{ old('seo_description') }}</textarea>
                    <small class="form-text">Leave empty to use excerpt</small>
                </div>
            </div>

            <div>
                <div class="sidebar-card">
                    <div class="card-header">Publish Settings</div>
                    <div class="card-body">
                        <div class="form-group">
                            <label for="status" class="form-label">Status *</label>
                            <select name="status" id="status" class="form-select" required>
                                <option value="draft" {{ old('status') == 'draft' ? 'selected' : '' }}>Draft</option>
                                <option value="published" {{ old('status') == 'published' ? 'selected' : '' }}>Published</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label for="published_at" class="form-label">Publish Date</label>
                            <input type="datetime-local" name="published_at" id="published_at" class="form-control" value="{{ old('published_at') }}">
                        </div>

                        <div class="form-group">
                            <label for="author" class="form-label">Author *</label>
                            <input type="text" name="author" id="author" class="form-control" value="{{ old('author', 'TPD') }}" required>
                        </div>

                        <div class="form-group" style="margin-bottom: 0;">
                            <div class="form-check">
                                <input type="checkbox" name="is_featured" id="is_featured" class="form-check-input" value="1" {{ old('is_featured') ? 'checked' : '' }}>
                                <label for="is_featured" class="form-check-label">Mark as Featured</label>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="sidebar-card">
                    <div class="card-header">Category & Tags</div>
                    <div class="card-body">
                        <div class="form-group">
                            <label for="category" class="form-label">Category *</label>
                            <select name="category" id="category" class="form-select" required>
                                <option value="">Select Category</option>
                                @foreach($categories as $cat)
                                    <option value="{{ $cat }}" {{ old('category') == $cat ? 'selected' : '' }}>{{ $cat }}</option>
                                @endforeach
                            </select>
                        </div>

                        <div class="form-group" style="margin-bottom: 0;">
                            <label for="tags" class="form-label">Tags</label>
                            <input type="text" name="tags" id="tags" class="form-control" placeholder="Laravel, PHP, Web Development" value="{{ old('tags') }}">
                            <small class="form-text">Comma-separated values</small>
                        </div>
                    </div>
                </div>

                <div class="sidebar-card">
                    <div class="card-header">Featured Image *</div>
                    <div class="card-body">
                        <div class="file-input-wrapper">
                            <input type="file" name="featured_image" id="featured_image" class="form-control" accept="image/*" required>
                            <small class="form-text">Max size: 2MB</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-primary">Create Blog</button>
            <a href="{{ route('admin.blogs.index') }}" class="btn-secondary">Cancel</a>
        </div>
    </form>
</div>
@endsection