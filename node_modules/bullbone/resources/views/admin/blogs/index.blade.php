@extends('layout.app')

@section('content')
<style>
    .blogs-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 35px;
        padding-bottom: 20px;
        border-bottom: 3px solid #1a1a1a;
    }

    .page-title {
        font-size: 32px;
        font-weight: 700;
        color: #1a1a1a;
        margin: 0;
        letter-spacing: -0.5px;
    }

    .alert-success {
        background: #1a1a1a;
        color: #ffffff;
        padding: 16px 20px;
        border-radius: 8px;
        margin-bottom: 25px;
        border-left: 4px solid #4a4a4a;
        animation: slideIn 0.3s ease;
    }

    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .btn-add-new {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: #1a1a1a;
        color: #ffffff;
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-size: 15px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .btn-add-new:hover {
        background: #000000;
        color: #ffffff;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
    }

    .table-wrapper {
        background: #ffffff;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        border: 1px solid #e0e0e0;
    }

    .custom-table {
        width: 100%;
        border-collapse: collapse;
        margin: 0;
    }

    .custom-table thead {
        background: #1a1a1a;
    }

    .custom-table thead th {
        color: #ffffff;
        padding: 18px 16px;
        text-align: left;
        font-weight: 600;
        font-size: 14px;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        border-bottom: 2px solid #000000;
    }

    .custom-table tbody tr {
        border-bottom: 1px solid #e0e0e0;
        transition: all 0.2s ease;
    }

    .custom-table tbody tr:hover {
        background: #f8f8f8;
    }

    .custom-table tbody tr:last-child {
        border-bottom: none;
    }

    .custom-table tbody td {
        padding: 16px;
        color: #333;
        font-size: 14px;
        vertical-align: middle;
    }

    .custom-table tbody td:first-child {
        font-weight: 600;
        color: #1a1a1a;
    }

    .blog-title {
        font-weight: 600;
        color: #1a1a1a;
        max-width: 300px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .blog-slug {
        color: #666;
        font-family: 'Courier New', monospace;
        font-size: 13px;
        background: #f5f5f5;
        padding: 4px 8px;
        border-radius: 4px;
        display: inline-block;
    }

    .action-buttons {
        display: flex;
        gap: 8px;
        align-items: center;
    }

    .btn-edit {
        background: #ffffff;
        color: #1a1a1a;
        border: 2px solid #1a1a1a;
        padding: 8px 16px;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
        cursor: pointer;
        display: inline-block;
    }

    .btn-edit:hover {
        background: #1a1a1a;
        color: #ffffff;
        transform: translateY(-1px);
    }

    .btn-delete {
        background: #1a1a1a;
        color: #ffffff;
        border: 2px solid #1a1a1a;
        padding: 8px 16px;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 600;
        transition: all 0.2s ease;
        cursor: pointer;
    }

    .btn-delete:hover {
        background: #000000;
        border-color: #000000;
        transform: translateY(-1px);
    }

    .delete-form {
        display: inline;
        margin: 0;
    }

    .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: #666;
    }

    .empty-state-icon {
        font-size: 48px;
        color: #ccc;
        margin-bottom: 16px;
    }

    .empty-state-text {
        font-size: 16px;
        color: #666;
        margin: 0;
    }

    @media (max-width: 768px) {
        .page-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 15px;
        }

        .custom-table {
            font-size: 13px;
        }

        .custom-table thead th,
        .custom-table tbody td {
            padding: 12px 8px;
        }

        .action-buttons {
            flex-direction: column;
            gap: 6px;
        }

        .btn-edit,
        .btn-delete {
            width: 100%;
            text-align: center;
        }
    }


    /* Add to existing pagination styles - around line 530 */
.page-item.disabled .page-link {
    background: #f3f4f6;
    color: #9ca3af;
    cursor: not-allowed;
    pointer-events: none;
}

.page-item.disabled .page-link:hover {
    background: #f3f4f6;
    color: #9ca3af;
    transform: none;
}
</style>

<div class="blogs-container">
    <div class="page-header">
        <h1 class="page-title">Manage Blogs</h1>
        <a href="{{ route('admin.blogs.create') }}" class="btn-add-new">
            <span>+</span>
            <span>Add New Blog</span>
        </a>
    </div>

    @if(session('success'))
        <div class="alert-success">{{ session('success') }}</div>
    @endif

    <div class="table-wrapper">
        <table class="custom-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Slug</th>
                    <th>Created</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @forelse ($blogs as $blog)
                    <tr>
                        <td>{{ $blog->id }}</td>
                        <td><span class="blog-title">{{ $blog->title }}</span></td>
                        <td><span class="blog-slug">{{ $blog->slug }}</span></td>
                        <td>{{ $blog->created_at->format('d M Y') }}</td>
                        <td>
                            <div class="action-buttons">
                                <a href="{{ route('admin.blogs.edit', $blog->id) }}" class="btn-edit">Edit</a>
                                <form action="{{ route('admin.blogs.destroy', $blog->id) }}" method="POST" class="delete-form">
                                    @csrf
                                    @method('DELETE')
                                    <button class="btn-delete" onclick="return confirm('Delete this blog?')">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr>
                        <td colspan="5" class="empty-state">
                            <div class="empty-state-icon">📝</div>
                            <p class="empty-state-text">No blogs found. Create your first blog to get started!</p>
                        </td>
                    </tr>
                @endforelse
            </tbody>
        </table>
    </div>
</div>
@endsection