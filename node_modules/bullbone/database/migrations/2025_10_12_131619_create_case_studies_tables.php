<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        // 1. Categories table
        Schema::create('case_study_categories', function (Blueprint $table) {
            $table->id();
            $table->string('name', 50)->unique();
            $table->string('slug', 50)->unique();
            $table->string('icon', 100)->nullable();
            $table->integer('display_order')->default(0);
            $table->timestamps();
        });

        // 2. Main case studies table
        Schema::create('case_studies', function (Blueprint $table) {
            $table->id();
            $table->string('title');
            $table->text('description');
            $table->string('image');
            $table->text('search_keywords')->nullable();
            $table->text('challenge')->nullable();
            $table->text('solution')->nullable();
            $table->text('results')->nullable();
            $table->string('project_date', 50)->nullable();
            $table->enum('status', ['draft', 'published'])->default('published');
            $table->boolean('is_featured')->default(false);
            $table->integer('display_order')->default(0);
            $table->timestamps();
        });

        // 3. Category pivot table (many-to-many)
        Schema::create('case_study_category_pivot', function (Blueprint $table) {
            $table->id();
            $table->foreignId('case_study_id')->constrained('case_studies')->onDelete('cascade');
            $table->foreignId('case_study_category_id')->constrained('case_study_categories')->onDelete('cascade');
            $table->timestamps();
        });

        // 4. Technologies table
        Schema::create('case_study_technologies', function (Blueprint $table) {
            $table->id();
            $table->foreignId('case_study_id')->constrained('case_studies')->onDelete('cascade');
            $table->string('name', 100);
            $table->string('icon', 100);
            $table->integer('display_order')->default(0);
            $table->timestamps();
        });

        // 5. Timeline table
        Schema::create('case_study_timelines', function (Blueprint $table) {
            $table->id();
            $table->foreignId('case_study_id')->constrained('case_studies')->onDelete('cascade');
            $table->string('date', 50);
            $table->string('title', 150);
            $table->text('description');
            $table->integer('display_order')->default(0);
            $table->timestamps();
        });

        // 6. Metrics table
        Schema::create('case_study_metrics', function (Blueprint $table) {
            $table->id();
            $table->foreignId('case_study_id')->constrained('case_studies')->onDelete('cascade');
            $table->string('number', 50);
            $table->string('label', 100);
            $table->integer('display_order')->default(0);
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('case_study_metrics');
        Schema::dropIfExists('case_study_timelines');
        Schema::dropIfExists('case_study_technologies');
        Schema::dropIfExists('case_study_category_pivot');
        Schema::dropIfExists('case_studies');
        Schema::dropIfExists('case_study_categories');
    }
};
